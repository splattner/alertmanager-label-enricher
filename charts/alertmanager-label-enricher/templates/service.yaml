apiVersion: v1
kind: Service
metadata:
  name: {{ include "alertmanager-label-enricher.fullname" . }}
  labels:
    {{- include "alertmanager-label-enricher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alertmanager-label-enricher.selectorLabels" . | nindent 4 }}
